name: Deploy FastAPI Backend to Render

on:
  push:
    branches:
      - main
    paths:
      - 'server/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Render Deploy Hook
        run: curl -X POST https://api.render.com/deploy/srv-d24muvfgi27c73b7c9fg?key=0uq5Mwiztyc
